{
  "name": "@agentdeploy-io/edge-ui-playground",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "preview:prod": "vite build && wrangler pages dev dist --port 5174",
    "deploy": "node scripts/deploy.mjs",
    "deploy:dry-run": "node scripts/deploy.mjs --dry-run",
    "deploy:pages": "npm run build && npx wrangler pages deploy dist"
  },
  "dependencies": {
    "@agentdeploy-io/agents": "^0.1.0",
    "agents": "^0.18.0",
    "partysocket": "1.3.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "wrangler": "^3.90.0"
  }
}
